[Unit]
Description=Nubix Cloud Sync Manager
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/local/bin/nubix --background
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
